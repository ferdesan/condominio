# URL base da API. Em desenvolvimento o Vite faz proxy de /api para o backend.
VITE_API_URL=/api/v1

# URL do servidor Socket.IO (deixe vazio para usar a mesma origem).
VITE_SOCKET_URL=

VITE_APP_NAME=Condominio SaaS
